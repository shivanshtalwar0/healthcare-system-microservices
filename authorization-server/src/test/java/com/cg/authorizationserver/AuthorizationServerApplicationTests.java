package com.cg.authorizationserver;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;


@SpringBootTest
class AuthorizationServerApplicationTests {

    @Test
    void contextLoads() {
//        String j=JWT.create().withIssuer("healthcare").withSubject("ram").sign(Algorithm.HMAC512("secure"));
//        System.out.println(j);
//        JWTVerifier jwtVerifier=JWT.require(Algorithm.HMAC512("secure")).withIssuer("healthcare").build();
//        System.out.println(jwtVerifier.verify(j).getHeader());
//        System.out.println(JWT.decode(j).getSubject());

    }

}
